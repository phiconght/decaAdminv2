export type HeroDetail = {
  badgeText?: string;
  title: string;
  subtitle?: string;
  primaryCtaLabel?: string;
  primaryCtaHref?: string;
  secondaryCtaLabel?: string;
  secondaryCtaHref?: string;
  backgroundImageUrl?: string;
  visible: boolean;
};

export type HeroForm = HeroDetail;
